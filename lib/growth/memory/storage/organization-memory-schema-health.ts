import "server-only"

/** GE-AIOS-17B — Organization memory schema probe. Delegates to @fuzor/memory. */

export { isPlatformOrganizationMemorySchemaReady as isOrganizationMemorySchemaReady } from "@fuzor/memory"
