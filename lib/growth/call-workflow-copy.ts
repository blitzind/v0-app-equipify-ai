/** Dialer integrations open externally — Growth coaching uses manual/provider/browser mic transcript input. */
export const GROWTH_CALL_DIALER_SAFETY_COPY =
  "Google Voice is used for dialing only. Realtime Coaching uses manual, provider, or browser mic transcript input. Audio is not stored."

export const GROWTH_CALL_DIALER_NEXT_STEP_COPY =
  "Call opened in {dialer}. Start Realtime Coaching when the call begins."

/** Browser mic capture is opt-in per session — audio bytes are never stored. */
export const GROWTH_CALL_AUDIO_CAPTURE_ENABLED = true

/** Product invariant: dispositions require explicit operator action. */
export const GROWTH_CALL_AUTO_DISPOSITION_ENABLED = false
