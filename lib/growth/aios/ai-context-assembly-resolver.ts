import "server-only"

/** GE-AIOS-2J — Entity metadata resolver. Delegates to @fuzor/context. */

export { resolvePlatformContextEntityMetadata as resolveAiContextEntityMetadata } from "@fuzor/context"
