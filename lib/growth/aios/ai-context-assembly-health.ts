import "server-only"

/** GE-AIOS-2J — Context Assembly health report. Delegates to @fuzor/context. */

export { evaluatePlatformContextAssemblyHealth as evaluateAiContextAssemblyHealth } from "@fuzor/context"

export type { PlatformContextAssemblyHealthReport as AiContextAssemblyHealthReport } from "@fuzor/context"
