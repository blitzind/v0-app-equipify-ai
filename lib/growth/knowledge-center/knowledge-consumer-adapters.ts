/** Phase GS-3B — Consumer-scoped knowledge retrieval adapters (client-safe). */

export {
  buildPlatformKnowledgeConsumerContext as buildConsumerContext,
  resolvePlatformKnowledgeConsumerRetrievalScope as resolveConsumerRetrievalScope,
  retrievePlatformKnowledgeForConsumer as retrieveKnowledgeForConsumer,
} from "@fuzor/knowledge"
