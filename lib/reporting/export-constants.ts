/**
 * Client-side export safety rails (Phase 63.1).
 * Server-side caps live on individual report APIs.
 */
/** Warn when exporting more than this many **rows** in the browser (feeds, etc.). */
export const CLIENT_CSV_EXPORT_ROW_WARN_THRESHOLD = 5_000
