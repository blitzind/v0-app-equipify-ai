"use client"

import { Background, BackgroundVariant } from "reactflow"

export function GrowthAutomationBackground() {
  return <Background variant={BackgroundVariant.Dots} gap={20} size={1} color="rgba(0,0,0,0.08)" />
}
